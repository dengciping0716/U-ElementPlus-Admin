import { withInstall, withNoopInstall } from '../../utils';
import CollapseWrap from './src/CollapseWrap.vue';

export const mCollapseWrap = withInstall(CollapseWrap);
export default mCollapseWrap;
